# 운영 인수인계 — 세종 플랫폼

**이 문서의 목적 하나:** 김철우 부장이 2주 자리를 비워도 다른 사람이 **살려 놓을 수 있게** 하는 것.
새 기능을 만드는 법은 여기 없다. 그건 저장소의 주석과 커밋 메시지에 있다.

마지막 손질: 2026-09-20. **숫자나 경로가 코드와 다르면 코드가 맞다.**

> **읽는 순서:** 급하면 [§7 고장났을 때](#7-고장났을-때--증상부터) 부터. 처음이면 §1 → §2 → §7.

---

## 1. 무엇이 어디에 있나

| 있는 곳 | 무엇 | 없으면 |
|---|---|---|
| **GitHub Pages** (`sejong21c.com`) | 플랫폼 화면 전부. 정적 HTML — 서버가 없다 | 화면이 안 뜬다 |
| **Firebase (`sejong-platform`)** | 로그인 · 모든 기록(Firestore). **무료 Spark** | 자료를 못 읽는다 |
| **Cloudflare Worker** (`sejong-ai-gateway`) | AI 중계 · 첨부 저장(R2) · 사내문서 검색 중계 | AI·첨부가 죽는다 |
| **맥미니** (`ssh mini`, 192.168.0.27) | 사내문서 색인 · 도면 그림 · 야간 백업 · 파이스 | AI 가 사내문서 근거를 못 단다(채팅·로그인은 산다) |
| **시놀로지 NAS** | 원본 도면·문서. **손대지 않는다** | — |
| 저장소 | `sejong-platform-v2`(회사) · `pais_project`(부장 개인) | — |

**중요한 경계:** 플랫폼은 회사 것, 파이스(PAIS)는 부장 개인 비서다. 지금은 연말 시연 때문에
부장 계정을 같이 쓴다. 닿는 곳은 **두 군데뿐**이다 — 게이트웨이가 파이스 `/api/rag/search` 를
부르는 것(검색 전용 토큰)과 백업의 구글 드라이브 업로드.

---

## 2. 열쇠가 어디 있나 (값은 여기 안 적는다)

| 열쇠 | 어디 | 무엇에 |
|---|---|---|
| `platform-sa.json` | 맥 `~/pais_project/data/` | Firestore **읽기 전용**(`pais-reader`). 백업·동기화·세기 |
| `platform-sa-write.json` | 맥 같은 곳, `chmod 600` | Firestore **쓰기**(`pais-restore`). **이관·복원만** |
| `migrate-token.txt` | 맥 같은 곳 | 게이트웨이 `/file/*` 서버 토큰 |
| `settings.json` 의 `ragToken` | 맥 같은 곳 | 게이트웨이 → 파이스 **검색 전용** 토큰 |
| 워커 시크릿 | Cloudflare 대시보드 | `FIREBASE_SA_KEY` · `PAIS_TOKEN` · `FILE_SIGN_KEY` · `MIGRATE_TOKEN` 등 |

**지켜야 할 것 둘**
- **백업에 쓰기 열쇠를 주지 않는다.** 읽기 전용이라 야간 배치에 버그가 나도 데이터를 못 건드린다.
  그게 안전장치다. `platform-backup --자체검사` 가 자기 소스를 읽어 이걸 강제한다.
- 워커 시크릿은 **파워셸 파이프로 넣지 않는다** — 끝에 줄바꿈이 붙어 조용히 401 이 난다.
  `npx wrangler secret bulk <json파일>` 로.

---

## 3. 맥미니에서 도는 것 (launchd)

```bash
ssh mini "launchctl list | grep com.pais"
```

| 이름 | 무엇 | 꼴 |
|---|---|---|
| `com.pais.server` | 파이스 본체 **8787** (8788 은 감시기 runner) | 상주 |
| `com.pais.ollama` | 임베딩 모델 `bge-m3` (11434) | 상주 |
| `com.pais.cloudflared` | 터널 — `pais.sejong21c.com` → 8787 | 상주 |
| `com.pais.tailscaled` | 사설망 | 상주 |
| `com.pais.omniroute` / `9router` / `demo` | AI 라우팅·시연 | 상주 |
| `com.pais.mounts` | NAS 마운트 (2분마다) | **주기** |
| `com.pais.keepalive` | 살아있나 확인 | **주기** |
| `com.pais.platform-backup` | **야간 백업 02:30** | **예약** |

> **PID 가 `-` 라고 죽은 게 아니다.** 주기·예약형은 돌고 바로 끝난다. 상주형만 PID 로 본다.
> (이걸 몰라서 멀쩡한 서비스가 늘 빨간불이었다 — 2026-09-20 에 검사를 고쳤다.)

**한 줄 건강검사** — 이것만 기억하면 된다:
```bash
ssh mini "zsh ~/pais_project/scripts/mini-check.sh"
```
서비스·터널·색인·백업까지 본다. **"전부 정상"** 이 아니면 아래 §7.

**되살리기:**
```bash
ssh mini "zsh ~/pais_project/scripts/mini-revive.sh"
```

---

## 4. 정기적으로 도는 것

| 때 | 무엇 | 어디 |
|---|---|---|
| **매일 02:30** | 플랫폼 백업(증분) · **일요일은 전체 대조** | 맥 launchd |
| 매일 09:00 | 마감·결재 알림 → 메신저 `ai-alerts` | 워커 크론 |
| 매일 07:20 | 플랫폼 → 옵시디언 볼트 동기화 | 맥 (파이스) |

백업 결과: 맥 `~/Library/Logs/pais-stack/platform-backup.log` ·
스냅샷 `~/pais_project/data/platform-backup/스냅샷/` · 구글 드라이브에도 올라간다.

---

## 5. 한도 — 여기서 사고가 난다

| | 한도 | 넘으면 |
|---|---|---|
| Firestore **읽기** | 5만 / 일 | 그날 내내 429. **한국시간 16~17시에 풀린다** |
| Firestore 쓰기 | 2만 / 일 | 같음 |
| Firestore **송신** | **10GiB / 월** | **그 달이 끝날 때까지 막힌다** ← 진짜 벼랑. Spark 유지로 정했으니 여기가 마지노선 |
| R2 | 유료 구독 | 10GB·A 100만·B 1000만 넘을 때만 과금 |

**절대 하지 말 것: 컬렉션을 통째로 여러 번 훑기.** 2026-09-19 와 09-20 두 번 태웠다.
두 번째는 같은 자료(`messages` 3,555건)를 네 번 읽어서였다.

이제 배치 스크립트에 **하루 장부**가 있다(`src/platform_sync.js` 의 `읽기기록`).
실행을 넘나들며 하루치를 세고 3만을 넘으면 스스로 멈춘다. 장부는 `data/platform-read-ledger.json`.

**먼저 세라:**
```bash
ssh mini "cd ~/pais_project && /usr/local/bin/node scripts/platform-count.mjs"
```
집계 질의는 1,000건당 읽기 1건이라 거의 공짜다. 모든 배치의 첫 줄.

---

## 6. 무엇이 무엇을 지키고 있나 (되돌리면 조용히 깨지는 것들)

돌려 보고 **전부 통과**가 아니면 손대지 말 것. 전부 파이어베이스를 안 건드린다.

```bash
cd "C:\Users\철우김\sejong-platform-v2" ; npm run rules
```
보안 규칙을 **에뮬레이터**에 걸고 실제로 읽고 써 본다(50개). Java 필요.
규칙을 고쳤으면 **이걸 통과한 뒤에** Firebase 콘솔에 붙여넣고 게시한다.

```bash
cd "C:\Users\철우김\sejong-platform-v2" ; node test/audit-live-check.mjs
```
**게시된 운영 규칙**에 진짜 토큰으로 부딪쳐 본다(10개). 파일과 콘솔이 어긋나는 게
2026-07-17 사고의 원인이었다.

| 그 밖에 | 무엇을 지키나 |
|---|---|
| `node test/audit.test.mjs` | 품질기록 변경 이력 배선(16) |
| `node test/attach-r2.test.mjs` | 첨부가 옛 조각 방식으로 되돌아가는 것 |
| `node test/pwa-w1.test.mjs` | Firestore 영속 캐시가 빠지는 것 |
| `node test/messenger-lib.test.mjs` | 메신저 커튼·readers |
| `node test/rag-scope-check.mjs` | AI 검색 권한 범위(배포된 워커) |
| `node gateway/worker-test.mjs` | 게이트웨이 전체(54) |
| `ssh mini "… node src/doc_index.js"` | 색인·범위·하이브리드 불변식 |
| `ssh mini "… node scripts/rag-eval.mjs"` | **검색 품질**. 17문항, 지금 17/17 |

---

## 7. 고장났을 때 — 증상부터

### 플랫폼이 로그인 화면에서 안 넘어간다 / 자료가 옛날 것이다
**십중팔구 Firestore 하루 읽기 한도다.** 한도가 차면 `getDocs` 가 오류로 끝나지 않고 **영영 안 끝난다**.
- 확인: `node scripts/platform-count.mjs` → "한도가 이미 찼습니다" 면 그것이다.
- **한국시간 16~17시에 저절로 풀린다.** 그때까지 배치를 돌리지 말 것.
- 화면에는 "저장된 자료로 열었습니다" 띠가 뜬다 — 캐시로 버티게 해 둔 것이다(정상).

### AI 비서가 사내문서 근거를 못 단다
맥이 꺼졌거나 터널이 끊긴 것이다. 채팅·로그인은 멀쩡하다.
```bash
ssh mini "zsh ~/pais_project/scripts/mini-check.sh"
```
- 터널만 죽었으면 `launchctl kickstart -k gui/$(id -u)/com.pais.cloudflared`
- 답변에 `source=vectorize(맥 실패로 물러섬)` 이 보이면 맥이 원인이다.

### 재부팅했더니 맥의 서비스가 다 죽었다
**macOS 큰 업데이트 뒤에 로그인 항목이 통째로 꺼진다.** launchd 에 등록은 돼 있는데 PID 가 없다.
FileVault 문제가 아니다(꺼져 있다).
```bash
ssh mini "zsh ~/pais_project/scripts/mini-revive.sh"
```
근본 해결은 화면이 필요하다 — **시스템 설정 → 일반 → 로그인 항목**에서 다시 켜야 한다.

### 첨부가 안 열린다
새 첨부는 R2 에 있다(`att/…`). 옛 것은 전부 옮겼다(2026-09-20, 1,141개).
```bash
cd "C:\Users\철우김\sejong-platform-v2" ; node test/r2-attach-check.mjs
```
9개가 다 통과하면 게이트웨이는 멀쩡하다. 사내에 `workers.dev` 가 막힌 PC 가 있다(2026-07-24) —
그 PC 에서만 안 되면 그 문제다.

### AI 답변의 도면이 안 보인다
주소가 맥이 서명한 것이고 **7일이면 만료된다.** 옛 대화의 그림은 캡션만 남는다 — 정상이다.
다시 물으면 새 주소로 나온다.

### NCR·CAR 저장이 안 된다 (403)
**판(rev)을 안 올린 것이다.** 옛 화면이 캐시에 남아 있으면 그렇다 — 새로고침 두 번.
규칙이 "저장할 때 판이 반드시 커져야 한다"를 강제한다(조용한 덮어쓰기 방지).

### 메신저가 안 뜬다 / 옛 화면이 나온다
캐시버스터 **일곱 군데**가 전부 같아야 한다(`index.html MESSENGER_BUILD` · `messenger.html` css·js ·
`messenger.js` 의 import 둘과 `빌드` · `sw.js 버전`). `node test/pwa-w1.test.mjs` 가 검사한다.
사용자 쪽은 **새로고침 두 번**(서비스워커가 두 번째에 갈린다).

---

## 8. 자료를 되돌려야 할 때

```bash
ssh mini "cd ~/pais_project && /usr/local/bin/node scripts/platform-restore.mjs --컬렉션 t_ncrs"
```
기본은 **연습(dry-run)** 이다. 무엇이 쓰일지 보여 주기만 한다. 실제로 하려면 `--진짜`.

- **없는 필드를 지우지 않는다** — 늘리거나 되돌릴 뿐, 없애지 않는다.
- 쓰려면 `platform-sa-write.json` 이 있어야 한다. 없으면 403 이 난다(그게 정상이다).
- **거울은 기계마다 따로다.** 복원은 **그 기계의** 거울을 읽는다. **맥이 진실이다.**
- 왕복이 되는지 먼저 증명: `platform-restore.mjs --증명` (시험 컬렉션만 쓴다).

---

## 9. 손대면 안 되는 것

- **시놀로지 NAS 설정** — 건드리지 않는다.
- **B등급(반출금지) 도면·마크업을 클라우드에 올리지 않는다.** 색인에 넣어야 하면
  `doc-index-cli.mjs --범위 비밀` 로. 그러면 AI 비서에게 어떤 경우에도 안 나간다.
- `roadmap.html` — 부장 승인 전에는 커밋하지 않는다.
- **force push 금지.** main 에 직접 커밋하고, 푸시 전에 `git fetch` 로 확인한다.
- `expense_data*.json` 의 카드번호 — 뒤 4자리만. 화면에 찍지 않는다.
- 부장에게 드리는 명령줄은 **`cd` 없이 절대경로 한 줄** — 파워셸에는 `&&` 가 없다.

---

## 10. 지금 열려 있는 일 (2026-09-20 기준)

| | 상태 |
|---|---|
| 메신저 2단계 — 옛 메시지 `readers` 백필 | 도구 완성, **안 돌림**. `readers-backfill.mjs --훑기` 부터 |
| 메신저 2단계 — 규칙·구독 전환 | 시험대 준비됨(`[2단계]` 로 시작하는 시험 넷을 뒤집는 일) |
| ~~Spark → Blaze~~ | **Spark 유지로 결정(9/21).** 장부·백업이 버팀목 · PITR 없음 = 최대 24시간 손실 감수 |
| pvcalc 세 코드 계산식 | 색인은 끝. 계산기는 숫자가 필요해 별도 작업 |
| 맥 로그인 항목 재설정 | 화면이 필요 — 부장 몫 |
| 옛 `ncrs` 컬렉션 잔재 | 조각은 정리됨. 기록 14건은 `t_ncrs` 와 중복 |

---

## 11. 한 번 더 — 제일 자주 쓸 세 줄

```bash
ssh mini "zsh ~/pais_project/scripts/mini-check.sh"
```

```bash
ssh mini "cd ~/pais_project && /usr/local/bin/node scripts/platform-count.mjs"
```

```bash
ssh mini "zsh ~/pais_project/scripts/mini-revive.sh"
```

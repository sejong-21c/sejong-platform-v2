/* 재료 데이터 형식 템플릿 — pvcalc 웹 버전
 *
 * 사용법: 이 파일을 web/materials.js 로 복사하고 회사 licensed Sec II-D
 * 사본의 값으로 채운다. materials.js 는 .gitignore 되어 있으므로
 * 저장소·공개 호스팅에 올라가지 않는다. 파일이 없으면 모든 응력 칸은
 * 지금처럼 수동 입력으로 동작한다.
 *
 * ── 반드시 지킬 것 ──────────────────────────────────────────────
 * 1) edition 에 판·개정년도·표 번호를 정확히 적는다. 이 문자열은 모든
 *    계산서 머리에 찍히므로, 판이 바뀌었는데 데이터가 그대로면 즉시 보인다.
 * 2) temps 는 오름차순. 데이터 범위 밖 온도는 외삽하지 않고 거부한다
 *    (조용히 틀린 값을 내는 것보다 안 나오는 게 낫다).
 * 3) 하나의 항목 = 하나의 재료 + 제품형태 + 두께구간. Sec II-D 는 같은
 *    재질도 판재/단조/배관, 두께 구간에 따라 S 가 다르므로 name 에 명시한다.
 * 4) 표 각주(예: 특정 온도 이상 장기 사용 제한)는 name 이나 note 에 남긴다.
 *    이 도구는 각주를 해석하지 않는다.
 * 5) 값이 없는 온도점은 null 로 둔다. 그 구간은 조회가 거부된다.
 *
 * 아래 숫자는 전부 형식 설명용 가짜값이다. 실제 값으로 교체할 것.
 */
window.PVCALC_MATERIALS = {
  edition: "예시 데이터 — 실제 Sec II-D 값으로 교체 필요",
  tempUnit: "C",
  materials: [
    {
      name: "[예시] 재질-규격 (판재, t ≤ 00mm)",
      note: "Table 0X, 각주 XX — 실제 표 위치를 여기에 적을 것",
      temps: [ 20, 100, 200, 300, 400 ],
      S:     [100, 100,  90,  80,  70],   // 허용응력
      Sy:    [200, 190, 175, 160, 145],   // 항복강도 (외압 계산용)
      Ey:    [200000, 195000, 188000, 180000, 170000],  // 탄성계수
    },
    {
      name: "[예시] 볼트재질-규격 (Sy 불필요 예시)",
      note: "볼트는 S 만 있으면 됨 — 없는 물성은 생략 가능",
      temps: [ 20, 200, 400 ],
      S:     [170, 170, 150],
    },
  ],
};

# 무료로 가는 저장·백업 구조 — 해결방안 (2026-09-19)

부장님 요청: "맥미니·맥북·데스크탑·NAS·구글 드라이브 80TB·미니컴퓨터를 써서, PLM까지 확장해도
실시간으로 자료가 오가고, 백업되고, 이미지도 들어가고, **무료로**. R2는 이미 결제. 꼭 필요하면 정액 한두 개, 월 2~3만원 안."

## 결론

1. **큰 것은 DB 밖으로, DB에는 작은 기록만.** 파일 원본은 구글 공유 드라이브(A급)·NAS(B급), 그림으로 박아야 하는 것만 R2, 파이어베이스에는 글·번호·이력만.
2. **배치 작업은 전부 맥미니.** 백업·첨부 이관·ASME 색인·썸네일 — 워커의 CPU·하위요청 제한, 파이어베이스 하루 한도를 맥미니가 대신 감당한다.
3. **실시간 채팅은 파이어베이스 무료 그대로.** 자체 서버로 옮기지 않는다 — 정전·인터넷·새벽 장애를 회사가 떠안게 된다.
4. **추가 지출 0원.** ASME 색인은 맥미니가 한다 — bge-m3·의미검색 엔진·터널이 이미 맥에 다 있다(아래 3단계). Vectorize 는 안 쓴다.
5. **순서는 백업 → 첨부 이관 → 색인 → PLM.** 첨부 이관이 끝나면 한도 사고가 구조적으로 불가능해진다.

## 지금 문제의 진짜 원인 — 실측

| 사실 | 어디서 확인 | 뜻 |
|---|---|---|
| 첨부가 `chunk__*`·`dwg_*` 문서로 DB 안에 base64로 들어 있다(개당 최대 700KB, 수만 건) | CLAUDE.md·9/19 백업 사고 | 훑기 한 번에 하루 읽기 5만 소진. 백업도 못 한다 |
| **메신저는 영속 캐시가 없다** — `getFirestore(fbApp)` (messenger.js:37). ITP·QA 모듈은 `persistentLocalCache`를 쓴다 | grep | 메신저만 접속마다 서버에서 다 다시 읽는다 |
| 부팅 1회 = 메시지 500 + 직원×2 + 방 + 프로젝트 ≈ **600 읽기** (messenger.js:1422~1441) | grep | 직원 30명 × 하루 6회 접속 = **10만8천 읽기 = 한도 2배** |
| 파이어베이스 Spark: 읽기 5만/일 · 쓰기 2만/일 · 송신 10GiB/월 · PITR 없음 | 9/19 확인 | 한도를 넘으면 그날 플랫폼 전체가 429 |
| Vectorize 무료 ≈ 4,880조각. ASME 서술형 8권만 약 1만 조각 | ASME-색인-조사.md | 클라우드 색인으로는 두 권만 → **맥 bge-m3 로 옮긴다**(3단계) |

→ **캐시 없이는 전사 상용이 불가능하다.** 첨부와 무관하게 접속 읽기만으로 한도를 넘는다.
**b41(2026-09-19)에서 영속 캐시를 넣어 이 줄은 해결됐다** — 이제 재접속은 바뀐 것만 받는다.

## 원칙 네 개

1. DB 문서는 **10KB 이하 글**만. 파일·그림·base64 금지.
2. 파일의 자리는 등급이 정한다: **A급(공유 가능) → 구글 공유 드라이브 · B급(반출금지) → NAS만 · `<img>`로 박을 것 → R2**.
3. 대량 조회는 **맥미니에서, 세고 나서, 상한을 두고.** 문서 수는 `getCountFromServer`(1,000건당 1읽기)로 먼저 센다.
4. 증명 안 한 백업은 백업이 아니다 — **지웠다 되살리는 시험**까지가 완료.

## 자원 배치

| 장비 | 역할 | 왜 |
|---|---|---|
| **맥미니 M4** (상시) | 야간 백업 · 첨부 이관 · ASME 조각내기·그림 추출 · 썸네일 생성 · 월간 점검 | 이미 24시간 켜져 있고 파이스가 돈다. 워커 제한이 없다 |
| **구글 공유 드라이브 80TB** | A급 파일 원본 · DB 백업 보관 | 회사가 이미 값을 치렀다. 사내·사외 어디서나 클릭하면 열린다. 퇴사해도 파일이 남는다 |
| **NAS(시놀로지)** | B급 도면·마크업 원본 (반출금지) — **구성 변경 없음** | 클라우드 금지 자료. 테일스케일로 외부 열람 |
| **R2** (결제 중) | `<img>`로 박아야 하는 것만: 썸네일 · ASME 그림 · 메신저 인라인 이미지 | 서명 URL이 이미 돈다. 10GB 무료 안 |
| **파이어베이스** (무료) | 메시지 · 도면번호·리비전·상태·이력 · 결재 · 일정 — 작은 글만 | 실시간·인증은 이게 제일 싸고 안 죽는다 |
| **Cloudflare Worker** | 문지기 — 토큰 검사 · 서명 · 검색 | 무료 |
| **데스크탑 AMD 32GB** | 백업 3번째 사본 · PDF→그림·DWG→썸네일 대량 변환 | 디스크가 넉넉하고 윈도우라 CAD 도구가 돈다 |
| **미니컴퓨터** | 사내 B급 열람 게이트(노드 게이트 + 테일스케일) | 조용하고 싸다. 설계서에 이미 있는 자리 |
| **맥북 M4** | 예비 · 현장 | 이동용 |

## 단계

### 0단계 — 0원, 한도 사고를 구조적으로 막는다 ✅ 완료 (b41)
- ✅ **영속 캐시**: `getFirestore` → `initializeFirestore(fbApp, { localCache: persistentLocalCache({ tabManager: persistentMultipleTabManager() }) })`.
  **index.html(플랫폼 본체)와 messenger.js 둘 다.** 플랫폼 안 메신저는 부모의 `fb.db` 를 쓰므로(`getFB`) index.html 쪽이 본체다.
  ITP·QA·ITP빌더·QA생성기 4개 모듈은 이미 이 방식이었다 — 본체와 메신저만 빠져 있었다.
- ✅ **되돌림 방지 불변식 6개** (`test/pwa-w1.test.mjs`): 두 파일 각각 영속 캐시·맨 `getFirestore` 금지·여러 탭 관리자.
- ✅ **`tools/count-collections.mjs`** — 컬렉션별 문서 수를 `getCountFromServer`(1,000건당 1읽기)로 센다. 모든 배치 작업의 첫 줄.
- ✅ 백업 바인딩은 **계속 꺼둔 상태.**
- ⏸ **방별 구독(W2-c)은 연말로 되돌림** — 캐시로 *비용* 이유가 사라졌다. 남은 건 보안 하나(아래 진행 기록).

### 1단계 — 백업: 맥미니 → 구글 드라이브 (1~2일)
- 맥미니 야간 작업(파이스 스케줄): **바뀐 것만** (`updatedAt > 마지막실행`) → JSON → 맥미니 디스크 → 구글 공유 드라이브 `백업/플랫폼/YYYY-MM-DD/`.
- `chunk__*`·`dwg_*`는 **제외**. 컬렉션마다 쪽수 상한(PAGE_SIZE 300·MAX_PAGES 30 방식).
- 월 1회 전체 스냅샷은 **문서 수를 센 뒤** 일요일 새벽에, 필요하면 며칠에 나눠서.
- 보관: 일 30벌 · 월 12벌. 사본 3벌 — 맥미니 · 드라이브 · 데스크탑(3-2-1).
- **복원 스크립트를 같이 만들고, 시험 컬렉션 하나를 지웠다 되살려 증명.**
- 하루 읽기 비용: 수백 건.

### 2단계 — 첨부를 DB 밖으로 (제일 중요, 2~4주 나눠서)
- **새 첨부는 오늘부터 드라이브로.** 플랫폼에 구글 로그인(GIS)이 이미 있다 — `drive.file` 권한 하나 추가. 업로드 → 공유 드라이브 → DB에는 `{fileId, 이름, 크기, 올린사람, 날짜}`만.
- **기존 조각은 맥미니가 하루 정해진 양만 이관**: 조립 → 드라이브 업로드 → fileId 기록 → 조각 삭제.
  하루 한도 세 가지를 다 본다: 읽기 5만 · 쓰기(삭제) 2만 · **송신 10GiB/월** — 조각이 700KB라 송신이 먼저 걸린다. 그래서 몇 주.
- 등급 태그가 B급인 첨부는 드라이브가 아니라 **NAS 경로**로.
- 끝나면: DB가 작아지고, 백업이 가벼워지고, 훑기 사고가 불가능해지고, PLM이 들어와도 DB가 안 큰다.

### 3단계 — ASME 색인 (0원, 맥미니가 한다)
처음엔 Workers Paid $5 를 권했으나 **낼 필요가 없다.** 맥에 이미 다 있다(2026-09-19 실측):

| 있는 것 | 확인 |
|---|---|
| 의미검색 엔진 `src/vault_semantic.js` — 볼트 6,800장, 외부 벡터DB 없이 내적으로 10ms | 코드 |
| **bge-m3** (1024차원·8,192토큰) 가 맥 Ollama 에 받아져 있음 — 게이트웨이 색인과 같은 모델 | `ollama list` |
| 맥으로 들어오는 터널 `pais.sejong21c.com` → 8787 | `~/.cloudflared/config.yml` |
| `searchMeaning()` 이 파이스 서버에 이미 물려 있음 | `server.js:36` |

```
직원 질문 → 메신저 → 게이트웨이(토큰 검사) → pais.sejong21c.com/rag/search → 맥 bge-m3 → 답
```

만들 것 셋:
1. **조각 색인** — 지금 엔진은 노트 1장 = 벡터 1개(앞 1,200자)라 700쪽 책엔 못 쓴다.
   `tools/rag-chunk.mjs` 가 만든 조각 1만 개를 각각 벡터로. 1만 × 1024 = 40MB, 검색은 여전히 20ms 안.
   임베딩은 맥 bge-m3 라 **0원·무제한** (조각 수 상한이 사라진다).
2. **맥 서버에 `/rag/search` 한 길** — `searchMeaning` 을 HTTP 로. 터널은 이미 있으니 설정 변경 없음.
3. **게이트웨이 `/rag/search` 를 맥으로 넘기기** — Vectorize 호출 한 줄을 fetch 한 줄로.

- 그림은 조각 옆에 파일 경로를 적어 두고 R2(이미 결제)에서 서명 URL 로 내준다.
- 조각내기·표 복원·그림 추출도 전부 맥에서 (워커 CPU·하위요청 제한 없음).
- 순서는 ASME-색인-조사.md 그대로: 서술형 → Section II 표 복원(눈 대조) → 그림.
- **감수하는 것**: 맥미니가 꺼져 있으면 AI 비서 답변에서 *사내문서 근거*만 빠진다.
  채팅·플랫폼·인증은 맥과 무관하게 그대로 돈다.

### 4단계 — PLM
- 도면 원본: **A급 드라이브 / B급 NAS.** DB에는 도면번호·리비전·상태·이력·위치만.
- **이미 색인된 DWG 224,306장은 복사하지 않는다** — NAS 경로를 가리킨다.
- 썸네일은 **열어 본 도면만** 만들어 R2에(전부 만들면 약 6.7GB, 게으르게 하면 수백 MB).
- 실시간: 메타 문서가 리스너로 즉시 뜨고, 파일은 클릭할 때 드라이브·NAS에서 받는다. 지금 메시지와 같은 방식.

## 돈

| 항목 | 월 | 상태 |
|---|---|---|
| R2 | 10GB·100만 작업까지 0원 | 결제 중 |
| 파이어베이스 Spark | 0원 | 유지 |
| 구글 드라이브 80TB | 0원 (워크스페이스에 포함) | 보유 |
| Cloudflare Workers | 0원 (Free) | 유지 |
| 맥미니 색인·임베딩 | 0원 | 전기값만 |
| **합계** | **추가 0원** | |

Workers Paid $5/월은 **안 켠다** — Vectorize 를 안 쓰면 낼 이유가 없다.

## 하지 말 것

- **파이어베이스 Blaze 전환** — 하루 지출 상한 기능이 없다. 코드가 한 번 돌면 청구가 얼마든 올라간다. 지금 구조로 무료 안에 들어가니 갈 이유가 없다.
- **실시간 채팅을 맥미니로** — 정전·인터넷 끊김·새벽 장애·보안 노출을 회사가 떠안는다. 맥미니는 배치 담당.
- **구글 드라이브를 `<img>`로** — 안 된다. 클릭해서 열기는 되고, 박아 넣기는 R2 몫.
- **NAS 구성 변경** — 백업 사본도 NAS에 두지 않는다(맥미니·드라이브·데스크탑으로 3벌).
- **세지 않고 배치 돌리기** — 9/19 사고.
- **Vectorize 로 ASME 색인** — 무료 4,880조각에 막혀 $5 를 내게 된다. 맥 bge-m3 면 공짜에 상한도 없다.

## 검증 (단계마다)

| 단계 | 재는 것 | 통과 기준 |
|---|---|---|
| 0 | 메신저 부팅 1회 서버 읽기(콘솔 네트워크·`SJM` 카운터) | 600 → 100 이하, 재접속은 바뀐 것만 |
| 1 | 백업 파일 존재·크기·날짜 / **복원 시험** | 지운 컬렉션이 되살아난다 |
| 2 | `chunk__*` 잔여 수 · 새 첨부의 fileId | 잔여 0 · 클릭하면 드라이브에서 열림(사내·사외) |
| 3 | 맥 색인 조각 수 · 실제 질문 5개(그림 포함) | 근거 문서·그림이 답변에 붙는다 |
| 4 | 도면 열기(A급 사외·B급 사내) · 썸네일 | B급이 클라우드에 안 나간다(로그) |

**월 1회 자동 점검**(파이스): 컬렉션별 문서 수 · 백업 최신 날짜 · R2 사용량 · 맥 색인 조각 수를 공지방에 올린다.
사람이 안 보면 또 태운다.

## 부장님 결정 필요

1. ~~Workers Paid $5~~ — **필요 없음으로 정리됨(2026-09-19).**
2. 0단계 착수 — **완료(2026-09-19, b41).** 아래 진행 기록 참고.
3. 공유 드라이브 구조 — 부서별로 하나씩인지, 프로젝트별인지(2단계 시작 전까지).

## 진행 기록

- **2026-09-19 · 0단계 (b41)** — 플랫폼(index.html)과 메신저에 Firestore **영속 캐시**를 넣었다.
  재접속 때 서버에서 바뀐 것만 받는다. 로컬 확인: `firestore/[DEFAULT]/sejong-platform/main` 생성됨.
  되돌림 방지 불변식 6개를 `test/pwa-w1.test.mjs` 에 박았다(맨 `getFirestore` 금지 포함).
  · **방별 구독(W2-c)은 연말 그대로 둔다.** 캐시가 들어가면서 *비용* 이유는 사라졌고,
    남은 이유는 보안(전 직원 기기가 모든 방의 최근 메시지를 받는다) 하나다 — 상용 전에 하면 된다.
